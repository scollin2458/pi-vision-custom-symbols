This folder contains the files for the `openlayers` custom symbol.

Configuration options include:
- Map view settings such as center, zoom, min zoom, and max zoom.
- Marker behavior settings including enable/disable markers, icon sizing, and selected marker actions.
- Selected marker popup settings including popup title, hide popup title, PI Vision display URL, popup link label, and target behavior.
- Control visibility settings for zoom controls and attribution.
